[package]
name = "nodestor-streaming"
version.workspace = true
edition.workspace = true
description = "Tensor Streaming Kernel — Double buffering, Modo Metralhadora, async pipeline"

[dependencies]
nodestor-core = { path = "../nodestor-core" }
nodestor-transport = { path = "../nodestor-transport" }
nodestor-vulkan = { path = "../nodestor-vulkan" }
tokio = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
zstd = "0.13"
ash = "0.38"

tempfile = { workspace = true }
nodestor-gdeflate = { version = "0.1.0", path = "../nodestor-gdeflate" }
